---
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  # TODO: Fix this - why local-cluster? (Changed it to default for now)
  name: default
  namespace: openshift-gitops
spec:
  clusterResourceWhitelist:
    #- group: 'argoproj.io/v1alpha1'
    #  kind: 'Application'
    - group: '*'
      kind: '*'
  destinations:
    - namespace: '*'
      server: '*'
  sourceRepos:
    - '*'