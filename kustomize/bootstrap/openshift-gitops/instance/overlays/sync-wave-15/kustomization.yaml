apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  # Set sync wave delay to 15 seconds
  - target:
      group: argoproj.io
      version: v1beta1
      kind: ArgoCD
    path: argocd-sync-wave-delay.yaml